import * as React from 'react';
import {Text} from 'react-native';

const WordsListScreen = ({navigation}) => {
    return(
        <Text>This is Words list screen</Text>
    );
};

export default WordsListScreen;